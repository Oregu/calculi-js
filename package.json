{
  "name": "lambda-calculus",
  "version": "0.0.1",
  "description": "Parsing and executing lambda calculus expressions.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/Oregu/js-lambda-calculus.git"
  },
  "scripts": {
    "run": "node src/calculus.js",
    "build": "mkdir -p target && node_modules/.bin/babel src/calculus.js -o target/calculus.js",
    "test": "mocha --compilers js:babel-core/register test/**/*.spec.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
